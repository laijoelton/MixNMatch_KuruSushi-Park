{
  "name": "park-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Grand Park Auto control centre - webhook listener, event log, park state machine",
  "engines": { "node": ">=22.5.0" },
  "scripts": {
    "start": "node --no-warnings src/server.js",
    "dev": "node --no-warnings --watch src/server.js",
    "sig": "node --no-warnings scripts/signature-report.js",
    "reset": "node --no-warnings scripts/reset-db.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
